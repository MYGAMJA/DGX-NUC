# 2026-04-08 변경사항 로그 (분리본)

이 문서는 2026-04-08에 적용/확인한 내용만 별도로 정리한 파일입니다.
기존 체크리스트 파일은 수정하지 않습니다.

## 1) 상황 요약

- Stage-A (biped PhysX) 학습은 완료됨.
- Stage-B (v6 PhysX) 자동 전환은 시도되었으나 시작 직후 실패.
- 실패 원인은 v6 구조에서 contact sensor body 매칭 실패로 확인.

## 2) 확인된 오류

Stage-B 로그(`/tmp/hylion_v6_physx_stageB.log`)에서 확인:

- `RuntimeError: Sensor at path ... could not find any bodies with contact reporter API`
- `Pattern ... did not match any rigid bodies`
- `AttributeError: 'NoneType' object has no attribute 'count'`

해석:

- 학습 불안정 문제가 아니라, 환경 초기화(센서 매칭) 단계 실패.
- v6 USD의 nested hierarchy와 contact sensor 경로/필터 매칭 불일치가 핵심.

## 3) 4월 8일 코드 변경 내역

### A. contact sensor 매칭 로직 패치 (IsaacLab 쪽)

대상 파일:

- `/home/laba/IsaacLab/source/isaaclab_physx/isaaclab_physx/sensors/contact_sensor/contact_sensor.py`

변경 요지:

- 기존: 첫 번째 parent prim 기준으로만 body 탐색.
- 변경: 모든 resolved parent prim을 순회해서 body 수집.
- nested hierarchy 대응 fallback 탐색 로직 보강.

목적:

- v6처럼 깊은 계층 구조에서도 sensor body를 찾도록 개선.

### B. v6 BG 환경 contact sensor 경로 조정

대상 파일:

- `δ3/hylion/env_cfg_BG.py`

변경 요지:

- contact sensor `prim_path`를 v6 nested ankle_roll 링크를 겨냥하는 형태로 조정.
- 시도 중 임시 우회안(센서 기반 reward 비활성)도 적용/검토함.

목적:

- Stage-B 초기화 단계에서 contact body 매칭 실패를 줄이고 학습 시작 가능성 확보.

## 4) 실행/검증 이력

- Stage-A 완료 체크포인트 확인:
  - `/home/laba/Berkeley-Humanoid-Lite/scripts/rsl_rl/logs/rsl_rl/biped/2026-04-06_15-27-27/model_5999.pt`
- Stage-B를 위 체크포인트로 재시작해 반복 검증.
- 결과: contact sensor 관련 매칭 오류가 지속되어 Stage-B 정상 학습 진입 실패.

## 5) 현재 결론

- Stage-B 실패 원인은 정책이나 체크포인트가 아니라 v6 자산-센서 매칭 이슈.
- 해결 방향은 아래 2가지 중 하나:
  1. v6 USD/URDF를 biped 스타일에 맞게 재구성(권장, 근본 해결)
  2. contact sensor 의존 항목을 임시 비활성하고 학습 먼저 진행(우회)

## 6) 다음 작업 제안 (4월 8일 이후)

- [ ] v6 contact body prim path를 명시적으로 확정(좌/우 발 링크 절대경로)
- [ ] `env_cfg_BG.py`에서 해당 경로 기반 sensor/filter 패턴 고정
- [ ] Stage-B 100~300 iter 스모크 테스트
- [ ] `Traceback/NaN` 없음 + iteration 증가 확인 후 장기 학습 전환

## 7) 참고 로그

- Stage-A log: `/tmp/bhl_biped_physx_stageA.log`
- Stage-B log: `/tmp/hylion_v6_physx_stageB.log`
- Pipeline log: `/tmp/physx_pipeline.log`
